<?php
declare(strict_types=1);

/**
 * Suma dous valores e devolve o resultado.
 *
 * @param float|int $a Primeiro valor.
 * @param float|int $b Segundo valor.
 * @return float|int Resultado da suma.
 */
function sumar(float|int $a, float|int $b): float|int
{
    return $a + $b;
}
